# Masked Whispers — mobile app (Capacitor)

iOS build repository. CI: Codemagic (codemagic.yaml) — builds, signs and
uploads to App Store Connect using the "MW ASC API Key" integration.

Android is built separately; its signing config is deliberately excluded.
